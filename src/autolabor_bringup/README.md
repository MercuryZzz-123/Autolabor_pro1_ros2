# autolabor_bringup

System startup package.

Planned scope:

- Provide top-level launch files for real robot startup.
- Combine description, drivers, mapping, localization, navigation, and RViz.
- Keep robot-specific runtime parameters in `config/`.
