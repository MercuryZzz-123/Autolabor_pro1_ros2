# autolabor_mapping

Mapping package.

Planned scope:

- Provide SLAM launch files.
- Store mapping parameters and RViz configurations.
- Prefer ROS 2 maintained mapping components such as `slam_toolbox` unless Cartographer compatibility is required.
