# autolabor_navigation

Navigation package.

Planned scope:

- Provide Nav2 launch files and parameter files.
- Store maps and navigation RViz configurations.
- Add custom planner, controller, or behavior plugins only when the platform needs behavior not covered by Nav2.
